"""Interlude: rotating cards in an AI coding tool's status line while it works.

The core (cards, deck, generation, rendering) is host-agnostic. Host-specific
behaviour lives under `interlude.agents.*`.
"""

__version__ = "0.2.0"
